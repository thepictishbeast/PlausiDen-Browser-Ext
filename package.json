{
  "name": "plausiden-browser-ext",
  "version": "0.1.0",
  "description": "Tier 0 plausible deniability browser extension -- pollutes browsing history, cookies, and ad profiles locally",
  "private": true,
  "scripts": {
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "bash ./scripts/test.sh",
    "build": "npm run build:chrome && npm run build:firefox",
    "build:chrome": "bash ./scripts/build-chrome.sh",
    "build:firefox": "bash ./scripts/build-firefox.sh",
    "build:wasm": "bash ./scripts/build-wasm.sh",
    "dev": "bash ./scripts/dev.sh",
    "clean": "rm -rf dist/"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.40",
    "@typescript-eslint/eslint-plugin": "^8.59.1",
    "@typescript-eslint/parser": "^8.59.1",
    "esbuild": "^0.28.0",
    "eslint": "^8.57.0",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=18"
  }
}
